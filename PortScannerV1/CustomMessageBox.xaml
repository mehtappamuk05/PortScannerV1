<Window x:Class="PortScannerV1.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Uyarı" SizeToContent="WidthAndHeight" MinWidth="350" MaxWidth="500" MinHeight="180"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown" Topmost="True">

    <Border Background="#16161E" CornerRadius="12" BorderBrush="#2A2A35" BorderThickness="2" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" Opacity="0.5" Direction="270" ShadowDepth="5"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock x:Name="txtIcon" Text="ℹ️" FontSize="20" Margin="0,0,10,0"/>
                <TextBlock x:Name="txtTitle" Text="BİLGİ" Foreground="#A0A0B0" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>


            <TextBlock x:Name="txtMessage" Grid.Row="1" Text="Buraya uyarı mesajı gelecek." Foreground="White" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,20" VerticalAlignment="Center"/>


            <Button x:Name="btnOk" Grid.Row="2" Content="TAMAM" Click="BtnOk_Click" Width="120" Height="35" HorizontalAlignment="Right"
                    Background="#1E1E28" Foreground="#00E5FF" BorderBrush="#3A3A45" BorderThickness="1" FontWeight="Bold" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>