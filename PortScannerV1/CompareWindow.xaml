<Window x:Class="PortScannerV1.CompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tarama Karşılaştırma" Height="500" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1E1E28"/>
            <Setter Property="Foreground" Value="#A0A0B0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#2A2A35"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#2A2A35"/>
        </Style>
    </Window.Resources>

    <Border Background="#16161E" CornerRadius="15" BorderBrush="#2A2A35" BorderThickness="2">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Üst Çubuk -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="Tarama Karşılaştırma" Foreground="#00E5FF" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="✕" Width="35" Height="30" Background="Transparent" Foreground="#FF1744" BorderThickness="0" FontSize="14" FontWeight="Bold" Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>

            <!-- Girdi Alanı -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="Şu anki sonuçları şununla karşılaştır:" Foreground="#A0A0B0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="cmbScans" Width="250" SelectionChanged="CmbScans_SelectionChanged" Background="#1E1E28" Foreground="Black"/>
            </StackPanel>

            <!-- Karşılaştırma Tablosu -->
            <Border Grid.Row="2" Background="#1E1E28" CornerRadius="8" Padding="10">
                <DataGrid x:Name="dgCompare" Background="Transparent" Foreground="#E0E0E0"
                          BorderThickness="0" HeadersVisibility="Column" GridLinesVisibility="None" RowBackground="Transparent"
                          AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="*"/>
                        <DataGridTextColumn Header="Eski Durum" Binding="{Binding OldStatus}" Width="*"/>
                        <DataGridTextColumn Header="Şu Anki Durum" Binding="{Binding NewStatus}" Width="*"/>
                        <DataGridTextColumn Header="Analiz Sonucu" Binding="{Binding Analysis}" Width="2.5*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Border>
</Window>